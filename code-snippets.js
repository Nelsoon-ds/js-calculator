/* 
function handleInput() {
  operand = calculatorInput.value;
  secondNum = calculatorInput.value;
  console.log("Operand got updated with:", operand);
} */

/* function doSomething() {
    console.log(input.valueOf());
}
console.log(input); */

// Calculator operation = a number, an operator, another number (3 + 5)
/* function operate(operator, firstNum, secondNum) {
    if (operator === '+') {
        return firstNum + secondNum;
    } else if (operator === '-') {
        return firstNum - secondNum;
    } else if (operator === '*') {
        return firstNum * secondNum;
    } else if (operator === '/') {
        return firstNum / secondNum;
    } else {
        return null; // Return null for unsupported operators
    }
} */

// DOM manipulation
/* document.addEventListener('DOMContentLoaded', function () {
    document.querySelector('#calculator-form').addEventListener('submit', function (e) {
        e.preventDefault();
        
        // Access the input value by its ID
        const numberInput = document.querySelector('#calculatorInput');
        console.log(numberInput.value);
    });
}); */

/* document.getElementById(
    "first"
  ).textContent = `First number: ${firstNum}`;
  document.getElementById(
    "second"
  ).textContent = `Second number: ${secondNum}`;
  document.getElementById(
    "result"
  ).textContent = `Result: ${firstNum}  ${result}`;

document
  .getElementById("-")
  .addEventListener("click", () => subtract(firstNum, secondNum));
document
  .getElementById("*")
  .addEventListener("click", () => multiply(firstNum, secondNum));

  //  */
